# PostgreSQL Database Configuration
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/o2ai_fax_automation

# PostgreSQL Connection Details (used by migration script)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=o2ai_fax_automation
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# For production, use a strong password and consider SSL:
# DATABASE_URL=postgresql://user:strong_password@hostname:5432/database?sslmode=require
